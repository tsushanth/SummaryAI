<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- Audio recording feature -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <!-- Telephony feature (optional - app works without it but enhanced with it) -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:name=".MeetingMindApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeetingMind"
        android:usesCleartextTraffic="true"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="31">

        <!--
        Foreground Service Permission Declarations for Play Store
        Purpose: Recording audio during meetings and phone calls, and media playback
        -->

        <!-- Microphone foreground service: Used for recording audio during meetings and phone calls -->
        <property
            android:name="android.app.PROPERTY_FOREGROUND_SERVICE_MICROPHONE_DECLARATION"
            android:value="Meeting Mind uses the microphone to record meetings and phone calls for transcription. Recording only occurs when the user explicitly starts a recording session." />

        <!-- Media playback foreground service: Used for playing back recorded audio -->
        <property
            android:name="android.app.PROPERTY_FOREGROUND_SERVICE_MEDIA_PLAYBACK_DECLARATION"
            android:value="Meeting Mind uses media playback to play recorded meetings and phone calls. Playback continues in the background so users can listen while using other apps." />

        <!-- Phone call foreground service: Used for VoIP calling -->
        <property
            android:name="android.app.PROPERTY_FOREGROUND_SERVICE_PHONE_CALL_DECLARATION"
            android:value="Meeting Mind uses VoIP calling to make phone calls through the app. This enables automatic recording and transcription of phone conversations." />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MeetingMind"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links for OAuth callbacks -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="summaryai"
                    android:host="calendar"
                    android:path="/connected" />
            </intent-filter>

            <!-- Supabase auth callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="summaryai"
                    android:host="auth"
                    android:path="/callback" />
            </intent-filter>
        </activity>

        <!-- Audio Recording Foreground Service -->
        <service
            android:name=".service.AudioRecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:description="@string/service_description_recording" />

        <!-- Media Session Service for audio playback -->
        <service
            android:name=".service.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            android:description="@string/service_description_playback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

    </application>

</manifest>
